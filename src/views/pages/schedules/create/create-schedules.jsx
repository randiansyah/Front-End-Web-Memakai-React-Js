import React from "react";
import { connect } from "react-redux";

const CreateSchedulesPage = (props) => {
  return <div>hello create schedules</div>;
};

const mapStateToProps = (state) => ({});

const mapDispatchToProps = {};

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(CreateSchedulesPage);
